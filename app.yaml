command: ["uvicorn", "server.main:app", "--host", "0.0.0.0", "--port", "8000"]

env:
  # LLM configuration
  - name: DATABRICKS_SERVING_ENDPOINT
    value: "databricks-gpt-5-2"

  # Workspace configuration
  - name: WORKSPACE_ID
    value: "605192141841889"  # Dogfood workspace ID
